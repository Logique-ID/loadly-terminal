# Loadly API key — Loadly dashboard → Account → API Key.
# Copy this file to .env and paste your key. .env is gitignored.
# In CI the same value comes from the LOADLY_API_KEY GitHub secret.
LOADLY_API_KEY=
